import 'package:cqut/pages/Data/repo_browser.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('repo browser compiles', () {
    expect(RepoBrowserPage, isNotNull);
  });
}
