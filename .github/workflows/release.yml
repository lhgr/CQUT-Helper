name: Build and Release APK

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'Tag name for release'
        required: true
        default: 'v0.0.0'
      skip_release:
        description: 'Skip creating release?'
        required: false
        default: 'false'

permissions:
  contents: write

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest
    
    env:
      TZ: Asia/Shanghai
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Timezone
        run: |
          sudo timedatectl set-timezone Asia/Shanghai
          date
          
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          
      - name: Get Dependencies
        run: flutter pub get

      - name: Decode Firebase Options
        env:
          FIREBASE_OPTIONS_BASE64: ${{ secrets.FIREBASE_OPTIONS_BASE64 }}
        run: |
          echo $FIREBASE_OPTIONS_BASE64 | base64 -d > lib/firebase_options.dart

      - name: Decode Google Services Json
        env:
          GOOGLE_SERVICES_JSON_BASE64: ${{ secrets.GOOGLE_SERVICES_JSON_BASE64 }}
        run: |
          echo $GOOGLE_SERVICES_JSON_BASE64 | base64 -d > android/app/google-services.json

      - name: Decode Keystore
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
        run: |
          echo $ANDROID_KEYSTORE_BASE64 | base64 -d > android/app/upload-keystore.jks

      - name: Create Key Properties
        env:
          ANDROID_KEY_PROPERTIES_STORE_PASSWORD: ${{ secrets.ANDROID_KEY_PROPERTIES_STORE_PASSWORD }}
          ANDROID_KEY_PROPERTIES_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PROPERTIES_KEY_PASSWORD }}
          ANDROID_KEY_PROPERTIES_KEY_ALIAS: ${{ secrets.ANDROID_KEY_PROPERTIES_KEY_ALIAS }}
        run: |
          echo "storePassword=$ANDROID_KEY_PROPERTIES_STORE_PASSWORD" > android/key.properties
          echo "keyPassword=$ANDROID_KEY_PROPERTIES_KEY_PASSWORD" >> android/key.properties
          echo "keyAlias=$ANDROID_KEY_PROPERTIES_KEY_ALIAS" >> android/key.properties
          echo "storeFile=upload-keystore.jks" >> android/key.properties
      
      - name: Check Files
        run: |
          ls -l android/app/google-services.json || echo "google-services.json NOT FOUND"
          ls -l lib/firebase_options.dart || echo "firebase_options.dart NOT FOUND"
        
      - name: Build Fat APK (Universal)
        run: |
          flutter build apk --release
          mkdir -p build/releases
          mv build/app/outputs/flutter-apk/app-release.apk build/releases/CQUT-Helper-universal.apk
        
      - name: Build Split APKs
        run: |
          flutter build apk --release --split-per-abi
          mv build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk build/releases/
          mv build/app/outputs/flutter-apk/app-arm64-v8a-release.apk build/releases/
        
      - name: Create Release
        uses: softprops/action-gh-release@v1
        if: |
          (startsWith(github.ref, 'refs/tags/') || 
          github.event_name == 'workflow_dispatch') &&
          (github.event_name != 'workflow_dispatch' || github.event.inputs.skip_release == 'false')
        with:
          tag_name: ${{ github.ref_name || github.event.inputs.tag_name }}
          name: Release ${{ github.ref_name || github.event.inputs.tag_name }}
          files: |
            build/releases/CQUT-Helper-universal.apk
            build/releases/app-armeabi-v7a-release.apk
            build/releases/app-arm64-v8a-release.apk
          generate_release_notes: true
          draft: false
          prerelease: false
          token: ${{ secrets.GITHUB_TOKEN }}
